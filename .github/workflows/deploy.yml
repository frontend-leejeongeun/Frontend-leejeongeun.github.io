name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main # 'main' 브랜치에 푸시할 때마다 배포 실행

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 체크아웃 리포지토리
      - name: Checkout code
        uses: actions/checkout@v2

      # Node.js 환경 설정
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "20"

      # 종속성 설치
      - name: Install dependencies
        run: |
          npm install

      # git 사용자 정보 설정
      - name: Set up git user
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

      # GitHub 리모트 URL을 GH_TOKEN을 사용하여 설정
      - name: Set git remote URL with GH_TOKEN
        run: |
          git remote set-url origin https://$GH_TOKEN@github.com/frontend-leejeongeun/frontend-leejeongeun.github.io.git

      # 빌드 및 배포
      - name: Build and Deploy
        run: |
          npm run build
          npm install gh-pages --save-dev
          npm run deploy
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }} # GitHub 토큰 (자동으로 제공됨)
